// Purpose: define the share data shape after the database choice is confirmed.
const shareModel = {
  // TODO: add the share model or schema for the selected database.
};

export default shareModel;
