// Purpose: contain auth business rules without touching Express req/res objects.
const authService = {
  // TODO: implement auth workflows after the auth strategy is confirmed.
};

module.exports = authService;
